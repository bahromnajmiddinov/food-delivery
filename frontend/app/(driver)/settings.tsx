import React from 'react';
import { View, Text } from 'react-native';
import SettingsScreen from '../(customer)/settings';

export default function DriverSettingsWrapper() {
  return <SettingsScreen />;
}
